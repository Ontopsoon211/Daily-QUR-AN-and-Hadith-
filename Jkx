export function getUtcDaySeed() {
  const now = new Date()

  return Date.UTC(
    now.getUTCFullYear(),
    now.getUTCMonth(),
    now.getUTCDate()
  )
}

export function getDailyIndex(arrayLength, offset = 0) {
  const seed = getUtcDaySeed()
  return (seed + offset) % arrayLength
}
